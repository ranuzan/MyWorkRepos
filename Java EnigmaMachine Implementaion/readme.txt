the following code implements an m3 engima machine functionality

to run the machine simply compile all the code files and uncomment relevent part in Main and run

in the Main there are:
 -run examples 
 -performence function histogram 
 -message decryption
 -user input function for tester to test the machine